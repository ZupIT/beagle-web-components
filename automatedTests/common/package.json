{
  "name": "common",
  "version": "1.0.0",
  "description": "runner",
  "main": "index.js",
  "scripts": {
    "test": "yarn run test:angular:9 && yarn run test:react && yarn run test:angular:8",
    "test:react": "cypress run --browser chrome --headless --env baseUrl=http://localhost:3000/?path=button",
    "test:angular:9": "cypress run --browser chrome --headless --env baseUrl=http://localhost:4200/?path=button",
    "test:angular:8": "cypress run --browser chrome --headless --env baseUrl=http://localhost:4201/?path=button"
  },
  "cypress-cucumber-preprocessor": {
    "step_definitions": "cypress/support/steps"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^5.2.0",
    "cypress-cucumber-preprocessor": "^2.5.5"
  }
}
